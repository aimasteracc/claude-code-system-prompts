<!--
name: 'Agent Prompt: Session memory update instructions'
description: Instructions for updating session memory files during conversations
ccVersion: 2.0.58
variables:
  - MAX_SECTION_TOKENS
-->
重要：本消息及这些说明不是实际用户对话的一部分。不要在笔记内容中包含对"记录笔记"、"会话笔记提取"或这些更新说明的任何引用。

根据上面的用户对话（排除此记录指令消息以及系统提示词、claude.md 条目或任何过去的会话摘要），更新会话笔记文件。

文件 {{notesPath}} 已经为你读取。以下是其当前内容：
<current_notes_content>
{{currentNotes}}
</current_notes_content>

你的唯一任务是使用 Edit 工具更新笔记文件，然后停止。你可以进行多次编辑（根据需要更新每个部分）——在单条消息中并行进行所有 Edit 工具调用。不要调用任何其他工具。

编辑的关键规则：
- 文件必须保持其确切结构，包括所有部分、标题和斜体描述
-- 永远不要修改、删除或添加节标题（以 '#' 开头的行，如 # Task specification）
-- 永远不要修改或删除斜体 _部分描述_ 行（这些是每个标题后面紧接着的斜体行——以下划线开头和结尾）
-- 斜体 _部分描述_ 是必须完全保留的模板说明——它们指导每个部分中应包含什么内容
-- 只更新每个现有部分内斜体 _部分描述_ 下方出现的实际内容
-- 不要在现有结构之外添加任何新部分、摘要或信息
- 不要在笔记中的任何地方引用此记录过程或说明
- 如果某个部分没有实质性的新见解需要添加，可以跳过更新。不要添加填充内容如"暂无信息"，如果适当的话，直接将部分留空/未编辑。
- 为每个部分写详细、信息密集的内容——包括文件路径、函数名、错误消息、确切命令、技术细节等具体信息。
- 对于"关键结果"，包含用户请求的完整、确切输出（例如，完整表格、完整答案等）
- 不要包含已经在上下文中 CLAUDE.md 文件中的信息
- 将每个部分保持在约 ${MAX_SECTION_TOKENS} 个令牌/词以下——如果某个部分接近此限制，通过淘汰不太重要的细节来压缩它，同时保留最关键的信息
- 专注于可操作的、具体的信息，这些信息有助于理解或重现对话中讨论的工作
- 重要：始终更新"当前状态"以反映最近的工作——这对压缩后的连续性至关重要

使用 Edit 工具，file_path：{{notesPath}}

结构保留提醒：
每个部分有两个部分必须完全按照它们在当前文件中出现的方式保留：
1. 节标题（以 # 开头的行）
2. 斜体描述行（标题后紧接的 _斜体文本_——这是模板说明）

你只更新这两个保留行之后出现的实际内容。以下划线开头和结尾的斜体描述行是模板结构的一部分，不是要编辑或删除的内容。

请记住：并行使用 Edit 工具并停止。编辑后不要继续。只包含来自实际用户对话的见解，绝不来自这些记录说明。不要删除或更改节标题或斜体 _部分描述_。
