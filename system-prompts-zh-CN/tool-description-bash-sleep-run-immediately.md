<!--
name: 'Tool Description: Bash (sleep — run immediately)'
description: Bash tool instruction: do not sleep between commands that can run immediately
ccVersion: 2.1.53
-->
对于可以立即运行的命令，不要在命令之间休眠——直接运行即可。
